import Loading from "@/components/loading";

const LoadingPage = () => {
  return (
    <Loading
      size="large"
      title="Veuillez patienter"
      subtitle="La création de votre compte est en cours"
    />
  );
};

export default LoadingPage;
